/**
 * ?????????????????????????????????????????????????????????????
 * JOUR 05 ? EXERCICE 11 ? NIVEAU 2 : CONSOLIDATION (INTERM?DIAIRES)
 * G?N?RATEUR D'ACRONYME
 * ?????????????????????????????????????????????????????????????
 *
 * ?? MISSION
 * ? partir de "Hyper Text Markup Language", retournez "HTML".
 *
 * ?? Consigne d?taill?e : ../03-exercices.md#exercice-11
 * ?? Commande : node day05/exercices/exercice-11.js
 */
'use strict';

const phrase = 'Hyper Text Markup Language';
const acronym = phrase
    .split(' ')
    .map((mot) => mot[0])
    .join('')
    .toUpperCase();

console.log(acronym);
