/**
 * ?????????????????????????????????????????????????????????????
 * JOUR 05 ? EXERCICE 15 ? NIVEAU 3 : D?FI (AVANC?S)
 * V?RIFICATEUR D'ANAGRAMME
 * ?????????????????????????????????????????????????????????????
 *
 * ?? MISSION
 * V?rifiez si "chien" et "niche" sont des anagrammes.
 *
 * ?? Consigne d?taill?e : ../03-exercices.md#exercice-15
 * ?? Commande : node day05/exercices/exercice-15.js
 */
'use strict';

function estAnagramme(a, b) {
    const normaliser = (mot) => mot.toLowerCase().split('').sort().join('');
    return normaliser(a) === normaliser(b);
}

console.log(estAnagramme('chien', 'niche'));
