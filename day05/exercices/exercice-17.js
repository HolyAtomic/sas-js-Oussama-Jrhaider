/**
 * ?????????????????????????????????????????????????????????????
 * JOUR 05 ? EXERCICE 17 ? NIVEAU 3 : D?FI (AVANC?S)
 * LE CHIFFRE DE C?SAR
 * ?????????????????????????????????????????????????????????????
 *
 * ?? MISSION
 * Cr?ez une fonction chiffrerCesar(texte, decalage) qui d?cale chaque lettre.
 *
 * ?? Consigne d?taill?e : ../03-exercices.md#exercice-17
 * ?? Commande : node day05/exercices/exercice-17.js
 */
'use strict';

function chiffrerCesar(texte, decalage) {
    return texte.split('').map((char) => {
        const code = char.charCodeAt(0);

        if (code >= 65 && code <= 90) {
            return String.fromCharCode(((code - 65 + decalage) % 26) + 65);
        }

        if (code >= 97 && code <= 122) {
            return String.fromCharCode(((code - 97 + decalage) % 26) + 97);
        }

        return char;
    }).join('');
}

console.log(chiffrerCesar('ABC', 1));
